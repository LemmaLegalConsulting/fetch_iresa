# fly.toml file generated for lemma_intake

app = "lemma-intake"
primary_region = "sjc"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "suspend"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]

[env]
  ENV = "production"
